import { useState, type FormEvent } from 'react';
import * as React from 'react';
import { Helmet } from 'react-helmet-async';
import { Eye, EyeOff, LockKeyhole, ArrowRight } from 'lucide-react';
import { Link, Navigate, useLocation, useNavigate } from 'react-router-dom';
import { useAuth } from '../auth/AuthProvider';
export default function Login() { const { login, isAuthenticated, isLoading } = useAuth(); const navigate = useNavigate(); const location = useLocation(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [show, setShow] = useState(false); const [error, setError] = useState(''); if (isLoading) return null; if (isAuthenticated) { return <Navigate to="/dashboard" replace />; } const from = (location.state as { from?: string } | null)?.from || '/dashboard'; const submit = async (event: React.FormEvent) => { event.preventDefault(); setError(''); try { await login(email, password); navigate(from, { replace: true }); } catch { setError('Invalid credentials. Please try again.'); } }; return <><Helmet><title>Studio login — KTUX</title><meta name="robots" content="noindex" /></Helmet><div className="login-page"><div className="login-art"><div className="login-orbit" /><span className="eyebrow">KTUX / PRIVATE STUDIO</span><h1>Where the<br /><span className="luxury gold">work compounds.</span></h1><p>One calm place to see the signals, stories and systems behind the studio.</p></div><div className="login-card"><Link to="/" className="brand"><span className="brand-mark">K</span><span><strong>KTUX</strong><small>STUDIO</small></span></Link><span className="eyebrow">Admin access</span><h2>Welcome back.</h2><p>Sign in to your studio workspace.</p><form onSubmit={submit}><label className="form-label">Email<input className="form-field" type="email" required value={email} onChange={(event) => setEmail(event.target.value)} placeholder="admin@ktux.com" /></label><label className="form-label">Password<div className="password-field"><input className="form-field" type={show ? 'text' : 'password'} required value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Your password" /><button type="button" aria-label={show ? 'Hide password' : 'Show password'} onClick={() => setShow(!show)}>{show ? <EyeOff size={16} /> : <Eye size={16} />}</button></div></label>{error && <span className="form-error">{error}</span>}<button className="button button-primary" type="submit">Enter workspace <ArrowRight size={16} /></button></form><span className="login-note"><LockKeyhole size={13} /> Memory-safe authenticated workspace</span><Link className="login-home" to="/">Return to public site</Link></div></div></>; }

